from setuptools import setup, find_packages

setup(
    name="neuralnetwork",
    version="1.0.0",
    description="Neural Network from scratch",
    author="Danny Antonelli",
    packages=find_packages(),
    install_requires=["numpy"],
    python_requires=">=3.10"
)
